const bucketList: string[] = ["Barcelona", "Tokyo", "Lake Como", "Paris", "Marseille"];

const luckyNumbers: number[] = [7, 13 ,97];

const favoritePeople: string[] = ["Leo", "Neco", "Steffen", "Ela"];

bucketList.pop();
luckyNumbers.pop();
favoritePeople.pop();

console.log(bucketList);
console.log(luckyNumbers);
console.log(favoritePeople);
